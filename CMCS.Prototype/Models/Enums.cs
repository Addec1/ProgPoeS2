namespace CMCS.Prototype.Models
{
    public enum ClaimStatus { Draft, Submitted, Verified, Approved, Rejected }
    public enum ApprovalStage { Coordinator, Manager }
    public enum Decision { Approved, Rejected }
}